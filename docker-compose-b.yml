version: "3.8"
services:
  vnstat-dashboard:
    build: .
    image: vnstat-dashboard:cn-fixed
    container_name: vnstat-dashboard
    ports:
      - "18880:80"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - /var/lib/vnstat:/var/lib/vnstat:ro
    restart: unless-stopped
